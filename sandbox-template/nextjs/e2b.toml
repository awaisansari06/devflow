# This is a config for E2B sandbox template.
# You can use template ID (t08nhl6j6pp88bcvqtbr) or template name (devflow-project) to create a sandbox:

# Python SDK
# from e2b import Sandbox, AsyncSandbox
# sandbox = Sandbox.create("devflow-project") # Sync sandbox
# sandbox = await AsyncSandbox.create("devflow-project") # Async sandbox

# JS SDK
# import { Sandbox } from 'e2b'
# const sandbox = await Sandbox.create('devflow-project')

team_id = "c948e04a-b844-432d-a20f-3177156eb250"
start_cmd = "/compile_page.sh"
dockerfile = "e2b.Dockerfile"
template_name = "devflow-project"
template_id = "t08nhl6j6pp88bcvqtbr"
